#include <iostream>
#include <string>
#include <vector>

std::vector<std::string> tokenize(const std::string& line){
  std::vector<std::string> token;
  for(std::string word : line) {
    if(word != ' ') {
      
    
  }
}

int main() {
  std::string sentence = "Anup is a programmer";
  //kens = tokenize(sentence);
  for(std::string word : tokens) std::cout << word << "\n";
  return 0;
}
